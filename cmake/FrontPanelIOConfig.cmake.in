get_filename_component(FrontPanelIO_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET FrontPanelIO::FrontPanelIO)
    include("${FrontPanelIO_CMAKE_DIR}/@PROJECT_NAME@-targets.cmake")
endif()